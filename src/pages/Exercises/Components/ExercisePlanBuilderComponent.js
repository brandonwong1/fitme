import React from 'react';
import {ReactSortable} from "react-sortablejs";

// TO IMPLEMENT: https://github.com/SortableJS/react-sortablejs

const ExercisePlanBuilderComponent = (props) => {

  const [exercisePlan, setExercisePlan] = React.useState([]);

  // Each item for ReactSortable requires an 'id' and a 'name'

  return (
    <div>

    </div>
  )

}

export default ExercisePlanBuilderComponent;